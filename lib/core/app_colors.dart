import 'package:flutter/material.dart';

class AppColors {
  // Surfaces and Backgrounds
  static const Color matteBlack = Color(0xFF181818);
  static const Color navBackground = Color(0xFF121212);
  
  // Brand Accent (Spotify-style green)
  static const Color accentGreen = Color(0xFF1DB954);
  
  // Faded Overlays
  static Color whiteFaded10 = Colors.white.withOpacity(0.1);
  static Color whiteFaded08 = Colors.white.withOpacity(0.08);
  static Color whiteFaded04 = Colors.white.withOpacity(0.04);
}